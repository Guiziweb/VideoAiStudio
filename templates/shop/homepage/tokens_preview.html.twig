<section class="py-5 bg-light">
    <div class="container">
        <div class="row justify-content-center text-center mb-5">
            <div class="col-12 col-lg-8">
                <h2 class="display-5 fw-bold">{{ 'app.homepage.tokens.title'|trans }}</h2>
                <p class="lead">{{ 'app.homepage.tokens.subtitle'|trans }}</p>
            </div>
        </div>

        {{ component('app:token_packs') }}
    </div>
</section>
