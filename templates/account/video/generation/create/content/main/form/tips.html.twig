<div class="col-lg-4">
    <div class="py-3">
        <div class="d-flex flex-column gap-3 text-muted small">
            <div class="d-flex align-items-center">
                {{ ux_icon('tabler:sparkles', { class: 'icon icon-xs text-muted me-2' }) }}
                <span>Be creative</span>
            </div>
            <div class="d-flex align-items-center">
                {{ ux_icon('tabler:target', { class: 'icon icon-xs text-muted me-2' }) }}
                <span>Be specific</span>
            </div>
            <div class="d-flex align-items-center">
                {{ ux_icon('tabler:palette', { class: 'icon icon-xs text-muted me-2' }) }}
                <span>Add style details</span>
            </div>
        </div>
    </div>
</div>